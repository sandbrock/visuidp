# Terraform files to ignore

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
# But keep the example files
*.tfvars
!dev.tfvars
!staging.tfvars
!prod.tfvars

# Ignore override files as they are usually used to override resources locally
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Backend configuration (contains sensitive information)
backend-config.hcl

# CLI configuration files
.terraformrc
terraform.rc

# Lock files (optional - some teams commit these)
# .terraform.lock.hcl
