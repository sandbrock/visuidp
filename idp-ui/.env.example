# Entra ID (Azure AD) Configuration
# These values should be obtained from your Entra ID app registration

# Tenant ID - The directory (tenant) ID from Entra ID
VITE_ENTRA_TENANT_ID=your-tenant-id-here

# Client ID - The application (client) ID from Entra ID app registration
VITE_ENTRA_CLIENT_ID=your-client-id-here

# Redirect URI - Must match the redirect URI configured in Entra ID app registration
# For local development: http://localhost:8083/ui
# For CloudFront deployment: https://your-domain.com/ui
VITE_ENTRA_REDIRECT_URI=http://localhost:8083/ui

# Development mode authentication groups (optional)
# Only used when running locally without Traefik
# Example: user,admin
VITE_DEV_AUTH_GROUPS=
